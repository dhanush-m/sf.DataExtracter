import { SFMC_Client } from '../../../types/sfmc_client';
/**
 * Gather dependency when Package Reference RegEx
 * @param reference
 * @param matchedValue
 * @param asset
 * @param assets
 * @returns
 */
declare const getAssetDependency: (client: SFMC_Client, reference: string, matchedValue: string, asset: any, packageOut: {
    contentBuilder?: {
        assets: [
            {
                id: number;
                name: string;
            }
        ];
    };
}) => Promise<any>;
declare const setUpdatedPackageAssetContent: (dependencyReference: {
    reference: string;
    bldrId: string;
}, matchedValue: string, assetContent: string) => string;
export { getAssetDependency, setUpdatedPackageAssetContent };
