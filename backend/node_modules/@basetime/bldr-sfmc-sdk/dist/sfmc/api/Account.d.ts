import { Client } from '../types/sfmc_client';
export declare class Account {
    client: Client;
    constructor(client: Client);
    getAccessTokenResponse(): Promise<any>;
    /**
     * Retrieve SFMC Account details for an instance
     * @returns
     */
    getInstanceDetails(): Promise<any>;
    /**
     * Get a single Business Units details
     *
     * @param {number} mid
     * @returns
     */
    getBusinessUnitDetails(mid: number): Promise<any>;
    getAllBusinessUnitDetails(mids?: number[]): Promise<any>;
}
