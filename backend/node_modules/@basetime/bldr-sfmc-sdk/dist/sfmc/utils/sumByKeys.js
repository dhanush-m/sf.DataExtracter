"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.concatByKey = exports.sumByKey = void 0;
const sumByKey = (arr, key) => {
    const res = arr.reduce((previousValue, currentValue) => {
        return {
            [key]: previousValue[key] + currentValue[key],
        };
    });
    return res[key];
};
exports.sumByKey = sumByKey;
const concatByKey = (arr, key) => {
    const res = arr.reduce((previousValue, currentValue) => {
        return {
            [key]: [...previousValue[key], ...currentValue[key]],
        };
    });
    return res[key];
};
exports.concatByKey = concatByKey;
